#!/usr/bin/env python3
# Helper script to convert line-wrapped paragraphs to blocked paragraphs.

import os
import argparse


def main(args):
    print("Implementation pending")


if __name__ == '__main__':
    # Create the argument parser.
    parser = argparse.ArgumentParser(
        description="Converts single line paragraphs to blocks and back.",
        epilog="Written to ease the Ben-Pete writing workflow",
    )

    # Add arguments

    # Parse the arguments and run main.
    main(parser.parse_args())
